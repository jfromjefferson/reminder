-- Generate model.g.dart
flutter packages pub run build_runner build --delete-conflicting-outputs

-- Generate key (play store)
keytool -genkey -v -keystore keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias keystore

Model typeId

Reminder - 1
Category - 2
Settings - 3